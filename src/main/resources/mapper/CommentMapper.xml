<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.CommentMapper">

    <resultMap id="BaseResultMap" type="com.blog.entity.Comment">
        <id property="commentId" column="comment_id"/>
        <result property="content" column="content"/>
        <result property="articleId" column="article_id"/>
        <result property="userId" column="user_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <resultMap id="DetailResultMap" type="com.blog.entity.Comment" extends="BaseResultMap">
        <result property="authorName" column="author_name"/>
        <result property="authorAvatar" column="author_avatar"/>
        <result property="replyToName" column="reply_to_name"/>
    </resultMap>

    <sql id="BaseColumns">
        comment_id, content, article_id, user_id, parent_id, create_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM comment WHERE comment_id = #{commentId}
    </select>

    <select id="selectByArticleId" resultMap="DetailResultMap">
        SELECT
            c.comment_id, c.content, c.article_id, c.user_id, c.parent_id, c.create_time,
            u.nickname AS author_name,
            u.avatar AS author_avatar,
            pu.nickname AS reply_to_name
        FROM comment c
        LEFT JOIN `user` u ON c.user_id = u.user_id
        LEFT JOIN `user` pu ON c.parent_id IS NOT NULL AND pu.user_id = (
            SELECT c2.user_id FROM comment c2 WHERE c2.comment_id = c.parent_id
        )
        WHERE c.article_id = #{articleId}
        ORDER BY c.create_time ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="commentId">
        INSERT INTO comment (content, article_id, user_id, parent_id)
        VALUES (#{content}, #{articleId}, #{userId}, #{parentId})
    </insert>

    <delete id="deleteById">
        DELETE FROM comment WHERE comment_id = #{commentId}
    </delete>

    <delete id="deleteByArticleId">
        DELETE FROM comment WHERE article_id = #{articleId}
    </delete>

    <delete id="deleteByParentId">
        DELETE FROM comment WHERE parent_id = #{parentId}
    </delete>

</mapper>
